version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.2
    commands:
      - echo "Installing dependencies"

  build:
    commands:
      - echo "Running build steps"

  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'